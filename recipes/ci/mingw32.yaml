#
#  MinGW32 CI build
#

root: true

inherit:
  - env::mingw32

depends:
  - planets::maketurn-bin
  - name: libs::afl-bin
    environment:
      LOCAL_OPENSSL: "true"
  - libs::openssl-bin

buildScript: "true"
packageScript: "true"
