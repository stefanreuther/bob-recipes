#
#  MinGW32 CI build
#

root: true

inherit:
  - env::mingw32

environment:
  LOCAL_OPENSSL: "true"
  LOCAL_SDL2: "true"

depends:
  - planets::maketurn-bin
  - libs::openssl-bin
  - libs::sdl2-bin
  - libs::afl-bin
  - planets::c2ng-bin

buildScript: "true"
packageScript: "true"
