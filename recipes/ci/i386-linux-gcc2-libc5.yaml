#
#  i386-Linux gcc-2.7 libc5 CI build
#

root: true

environment:
  PHOST_TARGET: linux-x86

inherit:
  - env::i386-linux-gcc2-libc5

depends:
  - planets::pdk-bin
  - planets::phost-pkg

buildScript: "true"
packageScript: "true"
