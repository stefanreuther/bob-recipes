# Environment: use configured host compiler as target
# For use with utilities that are needed during the build process

environment:
  CC: ${HOSTCC}
  CXX: ${HOSTCXX}
  CFLAGS: ${HOSTCFLAGS}
  CXXFLAGS: ${HOSTCXXFLAGS}
  LDFLAGS: ${HOSTLDFLAGS}
  CROSS_COMPILE: ""
  TARGET: "POSIX"
  RUN: ""
